@model IEnumerable<SanPham>
<div class="container">
   @*  <table class="table">
        <thead>
            <tr>
                <th>Tên sản phẩm</th>
                <th>Mô tả</th>
                <th>Giá</th>
                <th>Hình ảnh</th>
                <th>Thể loại</th>
                <th>
                    Chức năng
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)

            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.Description</td>
                    <td>@item.Price</td>
                    <td><img src="@item.ImageUrl" width="50%" /></td>
                    <td>@item.TheLoai.Name</td>
                    
                </tr>
            }
        </tbody>
    </table> *@
    <form asp-action="Index" method="get">
        <div class="row mb-3">
            <div class="col-md-4">
                <input type="text" name="searchString" class="form-control" placeholder="Tìm kiếm theo tên sản phẩm" />
            </div>
            <div class="col-md-4">
                <select name="sortOrder" class="form-control">
                    <option value="">Sắp xếp theo</option>
                    <option value="name_desc">Tên: A - Z</option>
                    <option value="name_desc">Tên: Z - A</option>
                    <option value="price">Giá: Thấp đến Cao</option>
                    <option value="price_desc">Giá: Cao đến Thấp</option>
                </select>
            </div>
            <div class="col-md-4">
                <button type="submit" class="btn btn-primary">Tìm Kiếm</button>
            </div>
        </div>
    </form>
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-3">
                <div class="card" style="width: 18rem;">
                    @* <img src="@item.ImageUrl" class="card-img-top" alt="@item.Name"> *@
                    <a asp-action="Details" asp-route-SanPhamId="@item.Id">
                        <img src="@item.ImageUrl" alt="Product image" class="product-image , card-img-top">
                    </a>
                    <div class="card-body">
                        <h5 class="card-title">
                            <a asp-action="Details" asp-route-SanPhamId="@item.Id">@item.Name</a>
                        </h5>
                        @* <h6 class="card-title">@item.Price</h6> *@
                        <h6 class="card-title">@String.Format("{0:N0}", item.Price) đ</h6>
                        @* <p class="card-text">@item.Description</p> *@
                        <div class="row">
                            <div class="col-6">
                                <a href="#" class="btn btn-primary">Thêm Vào Yêu Thích &#10084;</a>
                            </div>
                            <div class="col-6">
                                <a href="#" class="btn btn-danger">Mua Hàng <i class='fas fa-cart-plus'></i></a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>